# Data and secrets never belong in this repo.
.env
data/
*.db
*.db-wal
*.db-shm
spool.jsonl

__pycache__/
*.pyc
.DS_Store
.venv/

# launcher state
.run/
logs/

# tooling scratch
.playwright-mcp/
site/test-results/
site/playwright-report/
*.png
# …except the README's screenshot, which is generated from synthetic data
!docs/board.png
# …and the portal's images, which Amplify deploys from the repo
!site/*.png
!site/public/*.png

# Amplify backend — per-branch outputs and generated code are deploy products
node_modules/
amplify_outputs.json
.amplify/
.npm/

# Rust build output — a few hundred MB of it
agent/target/
server/target/
